"""Agent Office - a herdr plugin that draws the agent fleet as a pixel-art
office. Subscriber + OfficeState + tier 0/1/2 Renderer + jump, with colour
themes and a character per agent kind.

See docs/design.md for the design (source of truth).
"""

__version__ = "0.1.0"
